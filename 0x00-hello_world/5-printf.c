#include <stdio.h>

/**
 * main - this is main work
 *
 * Return: 0 when the function as concluded
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
